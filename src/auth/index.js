import * as types from "./types";
import * as actions from "./actions";
import * as selectors from "./selectors";
import authReducer from "./reducer";

const module = { authReducer, types, actions, selectors };

export default module;
